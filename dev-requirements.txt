# Development dependencies for local testing
# Install with: pip install -r dev-requirements.txt

# Production dependencies
-r api/requirements.txt

# Local dev server dependencies
flask>=3.0.0
flask-cors>=4.0.0
